@model BusinessLayer.FoodWeightType

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FoodWeightType</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.FoodWeightTypeName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FoodWeightTypeName)
            @Html.ValidationMessageFor(model => model.FoodWeightTypeName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


