@model IEnumerable<QuickySaleOnlineShopper.Models.SubFoodGroup>

@{
    ViewBag.Title = "SubGroup Foood Items";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SubFoodGroupName)
        </th>
        <th>
            Food Group Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubFoodGroupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FoodGroup.FoodGroupName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubFoodGroupId }) 
            @*@Html.ActionLink("Details", "Details", new { id=item.SubFoodGroupId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubFoodGroupId })*@
        </td>
    </tr>
}

</table>
<div>
    @Html.ActionLink("Back", "Index", "StoreManagerItems")
</div>